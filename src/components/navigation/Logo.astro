---
import { cn } from '../../utils/cn';

interface Props {
    class?: string;
    tagline?: boolean;
    dark?: boolean;
}

const { class: className, tagline, dark } = Astro.props;
---

<a href="/" class={cn("inline-flex flex-col mx-auto select-none tracking-wider font-planetkosmos duration-quick", (dark ? "text-dark-2 hover:text-dark-0" : "drop-shadow-md text-light-0 hover:text-accent"), className)}>
    santai.gg
    {
        tagline &&
        <span class="text-center -skew-x-30 text-sm font-dharmagothic">Spectre Divide Leaderboards</span>
    }
</a>